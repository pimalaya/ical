---
cairn: tasks
change: a-write-back-never-breaks-the-line
---

# Tasks

- [x] Copy a replayed parameter off the source line instead of re-encoding it
- [x] Escape a newline in a vCalendar 1.0 value rather than writing it raw
- [x] Keep the fuzzer's calendar as a merge test, plus both minimal cases
- [x] Check the other two merge artifacts and drop the ones that are fixed
- [x] Run the suites, a short fuzz confirmation, fmt and clippy
