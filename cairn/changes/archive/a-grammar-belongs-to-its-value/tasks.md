---
cairn: tasks
change: a-grammar-belongs-to-its-value
---

- [x] Add `IcalUtcOffset::seconds` and move `tz`'s private parser onto it
- [x] Add `IcalDuration::seconds` and `IcalDuration::from_seconds`
- [x] Point the JSCalendar import and export at them
- [x] Test both grammars, the round trip through a number included
- [x] Fold the spec and log the change
